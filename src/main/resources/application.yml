server:
  port: 8080

spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres?currentSchema=aw_cheetah
    username: postgres
    password: admin
  flyway:
    enabled: true
  jpa:
    show-sql: true
